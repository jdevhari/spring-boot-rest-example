machine:
  java:
    version: oraclejdk8
  services:
    - docker

deployment:
  dockerhub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push jdevhari/spring
